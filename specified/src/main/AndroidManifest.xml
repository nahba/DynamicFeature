<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="moc.nahba.specified">
    <dist:module dist:instant="false"
        dist:title="@string/title_specified">
        <dist:delivery>
            <dist:install-time>
                <dist:conditions>
                    <dist:device-feature dist:name="android.hardware.camera.front" />
                    <dist:device-feature dist:name="android.hardware.bluetooth_le" />
                </dist:conditions>
            </dist:install-time>
        </dist:delivery>
        <dist:fusing dist:include="false" />
    </dist:module>
    <application>
        <activity android:name=".SpecifiedActivity"/>
    </application>
</manifest>